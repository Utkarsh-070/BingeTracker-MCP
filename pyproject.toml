[project]
name = "mcp-demo"
version = "0.1.0"
description = "Add your description "
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiosqlite>=0.22.1",
    "fastmcp>=2.0,<3.0",
]
